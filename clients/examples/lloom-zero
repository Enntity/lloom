#!/bin/sh
set -eu
lloom_home="${LLOOM_HOME:-$HOME/.lloom}"
profile="$lloom_home/integrations/zero.env"
if [ ! -f "$profile" ]; then
  echo "LLooM profile missing: $profile" >&2
  echo "Run: lloom integrate zero --apply --yes" >&2
  exit 1
fi
. "$profile"
exec "${LLOOM_ZERO_BIN:-zero}" "$@"
