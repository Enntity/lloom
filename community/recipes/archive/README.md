# Hosted recipe archive

This directory mirrors immutable prior recipe versions from `recipes/archive/`. The active host index declares each archived path in its recipe entry; archived documents remain available for reproducibility but are not recommendation candidates.
